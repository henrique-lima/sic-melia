class Event:
	def __init__(self, id, title, category, labels, rank, start, end, location, state):
		self.id = id
		self.title = title
		self.category = category
		self.labels = labels
		self.rank = rank
		self.start = start
		self.end = end
		self.location = location
		self.state = state